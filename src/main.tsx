import * as React from 'react';
import * as ReactDOM from 'react-dom';

import { MemberListContainer } from './pages/member-collection'; 

ReactDOM.render(
  <MemberListContainer />,
  document.getElementById('root')
);
